[Unit]
Description=LMS Logger Daemon
After=network.target

[Service]
Type=simple
User=nobody
Group=nobody
EnvironmentFile=$RUNDIR/lmslogger.env
WorkingDirectory=$RUNDIR/src
ExecStart=$EXECCMD -m lmslogger.daemon

Restart=always
RestartSec=5

[Install]
WantedBy=default.target
